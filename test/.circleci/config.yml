version: 2.1

jobs:
  build-test-macos-big-sur:
    macos:
      xcode: 11.7
    steps:
      - checkout
      - run: echo 'Do something'
workflows:
  build_and_test:
    jobs:
    - build-windows
    - build-macos
    - build-linux